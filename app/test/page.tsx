import OrbitAnimation from './OrbitComponent';

export default function TestPage() {
  return (
    <div className="w-screen h-screen flex items-center justify-center bg-paper-white dark:bg-ink-blue">
      <OrbitAnimation />
    </div>
  );
}
